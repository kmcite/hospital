// part of '../patients.dart';

// final waitingPatients = listSignal(<Patient>[]);

// final activePatient = signal<Patient?>(null);

// final minorOtPatients = listSignal(<Patient>[]);
// final activeMinorOtPatient = signal<Patient?>(null);

// final opdReferralPatients = listSignal(<Patient>[]);

// final wardReferralPatients = listSignal(<Patient>[]);
// final activeWardPatient = signal<Patient?>(null);

// final externalReferralPatients = listSignal(<Patient>[]);

// final homeDischargedPatients = listSignal(<Patient>[]);

// final minorOtDischargedPatients = listSignal(<Patient>[]);
// final wardDischargedPatients = listSignal(<Patient>[]);

// final ignoredPatients = listSignal(<Patient>[]);

// final abandonedPatientCount = signal<int>(0);

// final consultedPatientCount = signal<int>(0);

// final minorOtTreatedPatientCount = signal<int>(0);
// final wardTreatedPatientCount = signal<int>(0);

// final missedReceptionPatientCount = signal<int>(0);

// final patientQueueTick = signal<int>(0);

// Timer? _patientArrivalTimer;
// Timer? _patientQueueTimer;

// const patientWaitLimit = Duration(seconds: 45);
