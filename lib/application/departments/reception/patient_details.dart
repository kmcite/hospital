import 'package:hospital/main.dart';

class PatientDetailsOverlay extends StatelessWidget {
  const PatientDetailsOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
