# Utils
## Extensions
### **`.text()`** extension on any dynamic type
### **`.pad()`** extension on any widget type
### **`.center()`** extension on any dynamic type
### **`.card()`** extension on any dynamic type
### **`print()`** function when debug is on
