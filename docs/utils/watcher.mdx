# UI previously called **`Watcher`**
- its an abstract widget used for automatically reacting to any signals used in the preceding tree.
- well if you want to minimize rebuilds into a specific branch we have **`GUI`** implementation.
> UnderConstruction