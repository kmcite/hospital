---

### Extensions

Some commonly used extensions that help reduce boilerplate and improve developer productivity:

* **`text()`** – Converts any object into a `Text` widget by displaying its `toString()` value.
* **`pad()`** – Provides a convenient way to apply padding to widgets.
* **`prind()`** – Simplifies debugging and logging by printing object values.

---
